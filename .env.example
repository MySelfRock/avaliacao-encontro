# ============================================
# CONFIGURAÇÕES DO SERVIDOR
# ============================================

# Ambiente (development | production | test)
NODE_ENV=development

# Porta do servidor
PORT=3001

# URL do frontend (usado para links em emails)
FRONTEND_URL=http://localhost:5173

# ============================================
# AUTENTICAÇÃO JWT
# ============================================

# JWT Secret - Chave secreta para assinatura de tokens de acesso
# CRÍTICO: Gere uma chave forte e única para produção!
# Exemplo: openssl rand -base64 64
JWT_SECRET=sua-chave-secreta-muito-forte-troque-em-producao-12345

# Tempo de expiração do token JWT (ex: 15m, 1h, 7d, 30d)
JWT_EXPIRES_IN=7d

# ============================================
# REFRESH TOKENS
# ============================================

# Refresh Token Secret - Chave diferente do JWT_SECRET
# CRÍTICO: Gere uma chave forte e única para produção!
REFRESH_TOKEN_SECRET=sua-chave-refresh-token-forte-troque-em-producao-67890

# Tempo de expiração do refresh token
REFRESH_TOKEN_EXPIRES_IN=30d

# ============================================
# COOKIES E SEGURANÇA
# ============================================

# Cookie Secret - Para criptografia de cookies e CSRF
# CRÍTICO: Gere uma chave forte e única para produção!
COOKIE_SECRET=sua-chave-cookie-secret-forte-troque-em-producao-abcde

# ============================================
# EMAIL - SENDGRID
# ============================================

# SendGrid API Key
# OBRIGATÓRIO para funcionalidade de reset de senha
# Obtenha em: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# Email remetente verificado no SendGrid
SENDGRID_FROM_EMAIL=noreply@seudominio.com

# Nome do remetente
SENDGRID_FROM_NAME=Sistema de Avaliações

# ============================================
# DEPRECATED (manter por compatibilidade)
# ============================================

# Token de administração antigo (não usar mais, substituído por JWT)
ADMIN_TOKEN=supersecreto123

# ============================================
# INSTRUÇÕES DE SEGURANÇA
# ============================================

# 🔐 PRODUÇÃO:
# 1. Gere chaves fortes aleatórias para JWT_SECRET, REFRESH_TOKEN_SECRET e COOKIE_SECRET
# 2. Configure SENDGRID_API_KEY com uma API key válida
# 3. Configure SENDGRID_FROM_EMAIL com um email verificado no SendGrid
# 4. Configure FRONTEND_URL com a URL real do seu domínio
# 5. Mude NODE_ENV para 'production'
# 6. NUNCA commite este arquivo com valores reais!

# 🔑 GERAR CHAVES SEGURAS:
# Linux/Mac: openssl rand -base64 64
# Node.js:   node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
