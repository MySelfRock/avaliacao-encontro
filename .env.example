# Porta do servidor (opcional)
PORT=3001

# Token de administração para rotas /api/admin (DEPRECADO - usar JWT)
# IMPORTANTE: Altere este valor em produção!
ADMIN_TOKEN=supersecreto123

# JWT Secret - Chave secreta para assinatura de tokens
# IMPORTANTE: Gere uma chave forte e única para produção!
# Exemplo: openssl rand -base64 32
JWT_SECRET=sua-chave-secreta-muito-forte-troque-em-producao-12345

# Tempo de expiração do token JWT (ex: 1h, 7d, 30d)
JWT_EXPIRES_IN=7d

# Ambiente (development | production)
NODE_ENV=development
